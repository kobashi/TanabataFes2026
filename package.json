{
  "name": "tanabata-fes-2026",
  "version": "1.3.0",
  "private": true,
  "description": "Local Tanabata wish submission, moderation, and projection mapping display.",
  "scripts": {
    "start": "node scripts/start-with-env.js",
    "dev": "node scripts/dev-with-env.js",
    "dev:isolated": "node scripts/dev-isolated.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/admin.js && node --check public/projection.js && node --check scripts/start-with-env.js && node --check scripts/dev-with-env.js && node --check scripts/dev-isolated.js && node --check scripts/backup-data.js && node --check scripts/smoke-test.js && node --check scripts/smoke-dev.js && node --check scripts/caddy-switch.js && node --check scripts/upstream-state.js",
    "backup": "node scripts/backup-data.js",
    "smoke": "node scripts/smoke-test.js",
    "smoke:prod": "TARGET=http://127.0.0.1:3000 node scripts/smoke-test.js",
    "smoke:dev": "node scripts/smoke-dev.js",
    "proxy:start": "caddy run --config Caddyfile --adapter caddyfile",
    "proxy:validate": "caddy validate --config Caddyfile --adapter caddyfile",
    "proxy:reload": "caddy reload --config Caddyfile --adapter caddyfile",
    "proxy:switch": "node scripts/caddy-switch.js"
  },
  "engines": {
    "node": ">=20"
  }
}
